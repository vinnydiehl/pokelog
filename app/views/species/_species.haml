.data-entry[species]
  .id= species.id
  .main-content
    .top
      .sprite= species.sprite
      .name= species.name
    .yields
      - species.yields.each do |stat, amt|
        .stat{class: stat} #{amt} #{format_stat stat}
